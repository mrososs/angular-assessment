import { AppOnlyNumbersDirective } from './app-only-numbers.directive';

describe('AppOnlyNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new AppOnlyNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
